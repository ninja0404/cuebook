# Database (Postgres) — required
DATABASE_URL=postgresql://user:password@localhost:5432/cuebook

# AI (Anthropic) — required
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# Cache (Redis) — optional
REDIS_URL=redis://localhost:6379
