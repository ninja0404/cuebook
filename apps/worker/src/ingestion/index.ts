// Ingestion — controlled data-source adapters (news API / RSS / exchange WS /
// on-chain / official announcements). Flow: fetch → dedupe → store → hand to AI
// synthesis. AI does NOT fetch; this layer does (tech-stack §2.3). Placeholder.
export {}
