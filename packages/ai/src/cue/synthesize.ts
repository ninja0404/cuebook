// Cue synthesis — turn a controlled, already-ingested event into a sourced Cue.
// AI only synthesizes; it does NOT fetch from the open web (tech-stack §2.3).
// The output schema (Zod) + prompt are ported from the prior validation once
// confirmed. Placeholder until then — see PRD §7.1.
export {}
