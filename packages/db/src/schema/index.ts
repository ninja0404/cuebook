// Drizzle schema. Tables are defined here once the §11-A scoring-core decision
// fixes Settlement / Reputation fields, then Room / Cue / Frame / Asset follow.
// Intentionally empty until then — see PRD §11. Do not guess the schema early.
export {}
